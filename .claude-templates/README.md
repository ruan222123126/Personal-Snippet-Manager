# Claude Code 项目模板使用指南

本目录包含可复用的 Claude Code 项目模板，用于快速在新项目中建立标准化的工作流程。

## 📁 模板文件清单

### 0. **一键安装脚本** (`install.sh`) ⭐ 新增
快速安装整个模板系统，自动创建所有目录和配置。

**使用方法**:
```bash
# 方法 1: 直接运行（如果模板已在本地）
bash .claude-templates/install.sh

# 方法 2: 从远程安装
curl -sSL https://your-repo/install.sh | bash
```

**自动化功能**:
- ✅ 自动创建所有必需目录（task/、finish_task/、report/、.claude/、.claude-duo/）
- ✅ 复制所有模板文件到正确位置
- ✅ 设置文件执行权限
- ✅ 测试通知系统

### 1. **项目初始化脚本** (`init-project.sh`) ⭐ 增强
完整的项目初始化脚本，带交互式提示和进度显示。

**使用方法**:
```bash
# 在项目根目录运行
bash .claude-templates/init-project.sh [项目名称]
```

**功能特性**:
- 🎨 彩色输出和进度显示
- 📁 自动创建完整目录结构
- 📋 智能文件复制（不覆盖已存在文件）
- 🔔 测试通知系统
- 📦 package.json 集成提示

**自动创建的目录**:
```
project/
├── task/              # 待处理任务
├── finish_task/       # 已完成任务
├── report/            # 完成报告
├── .claude/           # Claude 配置和工具
│   └── task-notify.sh # 通知脚本（自动复制）
├── .claude-duo/       # 双 Claude 协作系统
│   ├── start-monitoring.sh  # 监控脚本（自动复制）
│   └── queue/         # 审查队列
└── CLAUDE.md          # 项目文档（自动创建）
```

### 2. **报告模板** (`report-template.md`)
任务完成报告的标准格式模板，包含：
- 任务概述
- 完成的工作清单
- 验证结果
- 问题和解决方案
- 技术要点
- 后续建议

**使用方法**:
```bash
# 复制到项目的 report/ 目录
cp .claude-templates/report-template.md report/YYYY-MM-DD-任务名称.md
```

### 3. **检查清单模板** (`checklist-template.md`)
全面的任务完成检查清单，包含：
- 基础检查清单
- 双 Claude 系统检查清单
- 代码质量检查清单
- 数据库变更检查清单
- 安全检查清单

**使用方法**:
- 直接复制相关检查清单到任务文件或 CLAUDE.md
- 根据项目需求定制检查项

### 4. **通知脚本** (`task-notify.sh`) ⭐ 增强
跨平台桌面通知脚本，**现在支持自动创建目录**。

**特性**:
- ✅ 自动创建所有项目目录（task/、finish_task/、report/ 等）
- 🖥️ Linux (notify-send)
- 🍎 macOS (terminal-notifier, osascript)
- 💻 终端回退显示

**安装方法**:
```bash
# 1. 复制到项目目录（可选，install.sh 会自动处理）
cp .claude-templates/task-notify.sh .claude/task-notify.sh

# 2. 添加执行权限
chmod +x .claude/task-notify.sh

# 3. 测试通知（首次运行会自动创建目录）
./.claude/task-notify.sh "✅ 测试" "通知系统正常" "normal"
```

**使用示例**:
```bash
# 成功通知
./.claude/task-notify.sh "✅ 任务完成" "用户认证功能已实现" "normal"

# 警告通知
./.claude/task-notify.sh "⚠️ 注意" "数据库连接较慢" "normal"

# 错误通知
./.claude/task-notify.sh "❌ 错误" "构建失败" "critical"
```

### 5. **双 Claude 系统** (`duo-system-template.sh`) ⭐ 增强
双 Claude 协作系统的启动脚本模板，**现在支持自动创建目录**。

**特性**:
- ✅ 启动时自动创建所有项目目录
- 🔍 实时文件监控
- 📋 审查队列管理
- 🎨 彩色输出和状态显示

**安装方法**:
```bash
# 使用 install.sh 或 init-project.sh 会自动安装
# 或手动安装：

# 1. 创建 .claude-duo 目录（脚本会自动创建）
mkdir -p .claude-duo

# 2. 复制脚本
cp .claude-templates/duo-system-template.sh .claude-duo/start-monitoring.sh
chmod +x .claude-duo/start-monitoring.sh

# 3. 添加 npm 脚本（参考 duo-package.json）
```

**依赖安装**:
```bash
# Ubuntu/Debian
sudo apt-get install inotify-tools jq

# macOS
brew install jq

# 或使用模板中的 npm 脚本
npm run duo:install
```

### 6. **CLAUDE.md 模板** (`CLAUDE-template.md`)
项目文档模板，包含完整的结构和说明。

**使用方法**:
```bash
# 复制到项目根目录
cp .claude-templates/CLAUDE-template.md CLAUDE.md

# 根据项目定制内容
vim CLAUDE.md
```

### 7. **Package.json 配置** (`duo-package.json`)
双 Claude 系统的 npm 脚本配置。

**使用方法**:
将以下脚本添加到项目的 `package.json`:
```json
{
  "scripts": {
    "duo": "bash ./.claude-duo/start-monitoring.sh",
    "duo:install": "sudo apt-get install -y inotify-tools jq",
    "duo:clean": "rm -rf ./.claude-duo/queue/*.json"
  }
}
```

## 🚀 快速开始

### ⭐ 方案 0: 一键安装（最简单）⭐ 推荐

```bash
# 在新项目目录中运行
bash .claude-templates/init-project.sh

# 或使用一键安装
bash .claude-templates/install.sh
```

**就这么简单！** 脚本会自动：
- ✅ 创建所有必需目录（task/、finish_task/、report/ 等）
- ✅ 复制所有模板文件
- ✅ 设置文件权限
- ✅ 测试通知系统

### 方案 1: 完整集成（手动）

```bash
# 1. 复制所有模板到项目
cp -r .claude-templates /path/to/new-project/.claude-templates

# 2. 运行初始化脚本（自动创建目录）
cd /path/to/new-project
bash .claude-templates/init-project.sh

# 3. 根据项目定制 CLAUDE.md
vim CLAUDE.md
```

### 方案 2: 按需使用

```bash
# 仅使用报告模板
cp .claude-templates/report-template.md report/template.md

# 仅使用检查清单
cp .claude-templates/checklist-template.md .claude/checklist.md

# 仅使用通知系统（首次运行会自动创建目录）
cp .claude-templates/task-notify.sh .claude/task-notify.sh
chmod +x .claude/task-notify.sh
```

## 📋 模板定制指南

### 步骤 1: 基础信息定制

在 `CLAUDE-template.md` 中填写：
- 项目名称和描述
- 技术栈列表
- 项目根目录路径
- 团队规范和标准

### 步骤 2: 目录结构定制

根据项目实际结构调整：
- 源代码目录
- 测试目录
- 配置文件位置
- 构建输出目录

### 步骤 3: 命令脚本定制

更新 `package.json` 中的脚本：
- 开发服务器启动命令
- 构建命令
- 测试命令
- 代码检查命令

### 步骤 4: 通知系统配置

如果需要自定义通知：
- 修改通知脚本中的图标路径
- 调整通知显示方式
- 添加项目特定的通知类型

### 步骤 5: 检查清单定制

根据项目需求：
- 添加项目特定的检查项
- 移除不相关的检查项
- 调整检查项的顺序

## 🎯 使用场景

### 场景 1: 新项目初始化 ⭐ 最常用

```bash
# 方法 A: 使用一键初始化（推荐）
cd /path/to/new-project
bash .claude-templates/init-project.sh

# 方法 B: 使用快速安装
cd /path/to/new-project
bash .claude-templates/install.sh

# 完成！所有目录和文件都已准备好
```

### 场景 2: 现有项目迁移

```bash
# 复制模板到现有项目
cp -r .claude-templates /path/to/existing-project/.claude-templates

# 运行初始化（不会覆盖现有文件）
cd /path/to/existing-project
bash .claude-templates/init-project.sh

# 定制 CLAUDE.md
vim CLAUDE.md
```

### 场景 3: 多项目统一管理

```bash
# 创建共享模板目录
mkdir -p ~/shared-templates
cp -r .claude-templates/* ~/shared-templates/

# 在各个项目中使用符号链接
cd /path/to/project1
ln -s ~/shared-templates .claude-templates
bash .claude-templates/init-project.sh

# 统一更新所有项目
cd ~/shared-templates
git pull  # 或其他更新方式
```

## 🔧 高级定制

### 添加项目特定模块

在 `CLAUDE-template.md` 中添加：

```markdown
### [模块名称]

**重要**: [模块说明]

#### 工作流程
[详细步骤]

#### 检查清单
- [ ] 检查项 1
- [ ] 检查项 2

#### 常用命令
```bash
[命令列表]
```
```

### 自定义报告模板

根据项目需求修改 `report-template.md`：
- 添加特定字段（性能指标、测试覆盖率等）
- 调整报告结构
- 添加项目特有的章节

### 扩展通知系统

修改 `task-notify.sh` 添加：
- 钉钉/企业微信通知
- Slack/Discord 集成
- 邮件通知
- 日志记录

## 📚 相关资源

- **Claude Code 官方文档**: https://claude.ai/code
- **项目模板仓库**: [您的仓库地址]
- **使用指南**: 本文件

## 🤝 贡献

如果您有改进建议或发现了问题：

1. 修改模板文件
2. 更新本使用指南
3. 提交改进建议

## 📝 版本历史

- **v1.0.0** (2025-01-24)
  - 初始版本
  - 包含 6 个核心模板
  - 完整的使用指南

---

**注意**: 使用前请仔细阅读每个模板文件中的注释和说明，根据项目需求进行适当调整。
