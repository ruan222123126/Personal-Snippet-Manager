# .moon/toolchain.yml

# ❌ 注释掉整个 Node 配置，彻底禁止 Moon 干预 Node 版本
# node:
#   version: 'system'
#   packageManager: 'npm'
#   addEnginesConstraint: false
#   syncProjectWorkspaceDependencies: true

# TypeScript 配置可以保留，或者也注释掉（如果你不需要 Moon 帮你同步 tsconfig）
typescript:
  createMissingConfig: true
  routeOutDirToCache: true
  syncProjectReferences: true